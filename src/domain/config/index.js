import HOST_APP_DATA from './host-app-data.json'

const config = {
  userEmail: 'averylongemailaddress@companyname.com',
  MAX_APPS_IN_CARD_LENGTH: 5,
  MAX_TOP_APPS_BY_HOST_LENGTH: 25,
  HOST_APP_DATA
}

export {config}
